version: 2
updates:
  # Go module updates are handled by MintMaker/Renovate (Konflux platform bot).
  # Dependabot gomod entries were removed to eliminate duplicate PRs — see issue #108.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    open-pull-requests-limit: 5
